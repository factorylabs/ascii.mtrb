require 'rake'

task 'db:migrate' do
  `bundle exec jekyll`
end

# check process ascii_node
        # with pidfile "/var/run/ascii_node.pid"
        # start program = "/data/nodejs/node /data/ascii/current/telnet.js"
        # stop program = "/bin/bash -c '/usr/bin/kill -TERM `cat /var/run/ascii_node.pid`"
        # group ascii_node

